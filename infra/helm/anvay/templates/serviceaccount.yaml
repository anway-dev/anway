apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Release.Name }}-gateway
  annotations:
    # IRSA annotation placeholder — enable for AWS IAM Roles for Service Accounts
    # eks.amazonaws.com/role-arn: arn:aws:iam::<account>:role/anvay-gateway
    {{- with .Values.serviceAccount.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
